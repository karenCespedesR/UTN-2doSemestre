// Ejercicio : Calcular la suma de  n primeros numeros
let nro = parseInt(input("Digite la cantidad de nros a sumarse"));
let suma = 0;
for (let i = 0; i <= nro; i++) {
    suma += i;
}
console.log("La suma es : " + suma);
